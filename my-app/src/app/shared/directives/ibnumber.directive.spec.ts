import { IbnumberDirective } from './ibnumber.directive';

describe('IbnumberDirective', () => {
  it('should create an instance', () => {
    const directive = new IbnumberDirective();
    expect(directive).toBeTruthy();
  });
});
